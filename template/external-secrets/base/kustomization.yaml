apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: external-secrets

resources:
  - repository.yaml
  - release.yaml
  - certificate.yaml

configMapGenerator:
- name: values
  files:
  - values.yaml

configurations:
- kustomizeconfig.yaml