apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: istiod
spec:
  interval: 30m
  chart:
    spec:
      chart: istiod
      version: 1.29.2
      sourceRef:
        kind: HelmRepository
        name: istio
        namespace: istio-system
      interval: 12h
  dependsOn:
  - name: istio-base
    namespace: istio-system
  # valuesFrom:
  #   - kind: ConfigMap
  #     name: values
